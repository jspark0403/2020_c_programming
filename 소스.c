#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

/*

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    if (a == 0) {
        if (b == 0)
            printf("公铰何");
        else if (b == 1)
            printf("铰府");
        else
            printf("菩硅");
    }

    if (a == 1) {
        if (b == 0)
            printf("菩硅");
        else if (b == 1)
            printf("公铰何");
        else
            printf("铰府");
    }
    if (a == 2) {
        if (b == 0)
            printf("铰府");
        else if (b == 1)
            printf("菩硅");
        else
            printf("铰府");
    }

}

*/

/*

int main() {
    int iron, divide, total, i;

    scanf("%d %d", &iron, &divide);

    total = iron / divide;

    for (i = 0; i < total; i++)
        printf("%d ",i);
    
    return 0;

}

*/

/*

int main() {

    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);

    for (int i = 1000; i >= 1; i--) {
        if (a % i == 0 && b % i == 0 && c % i == 0) {
            printf("%d", i);
            break;
        }
    }

    return 0;
}

*/

int main() {
    long long a;
    int i;
    int b;
    scanf("%d", &a);
    for (i = 0; i < 10; i++) {
        
    }
}